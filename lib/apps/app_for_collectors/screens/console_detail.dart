import 'package:flutter/material.dart';


class ConsoleDetail extends StatelessWidget {
  const ConsoleDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}